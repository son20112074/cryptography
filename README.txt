Fix in head file to run with new test:
	N: A RSA number that you want to factoring.
	n_smooth: is first n primes to create a factor base set.
	offset: A number set from sqrt(N) to (sqrt(N) + offset) to find a square product.